<p id="notice"><%= notice %></p>

<h1>Manager Admins</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @manager_admins.each do |manager_admin| %>
      <tr>
        <td><%= link_to 'Show', manager_admin %></td>
        <td><%= link_to 'Edit', edit_manager_admin_path(manager_admin) %></td>
        <td><%= link_to 'Destroy', manager_admin, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Manager Admin', new_manager_admin_path %>
