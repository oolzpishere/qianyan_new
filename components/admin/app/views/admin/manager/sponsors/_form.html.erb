<%= form_with(model: manager_sponsor) do |form| %>
  <% if manager_sponsor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(manager_sponsor.errors.count, "error") %> prohibited this manager_sponsor from being saved:</h2>

      <ul>
        <% manager_sponsor.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
